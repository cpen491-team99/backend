# MQTT over TCP (for backend / CLI clients)
listener 1883 0.0.0.0
protocol mqtt

# MQTT over WebSockets (for browser frontend)
listener 9001 0.0.0.0
protocol websockets

# Allow anonymous access for local dev (OK for now)
allow_anonymous true

# Don't persist messages in dev
persistence false